# IMDB-Crawler

Electron + React app to query IMDb datasets locally and export structured data.

Docs are in the `docs/` folder. See `docs/development-plan.md` for the roadmap and `docs/dev-setup.md` for setup instructions.
